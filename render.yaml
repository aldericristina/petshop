services: 
  - type: web
    name: exercicio3
    runtime: node 
    plan: free
    buildCommand: "npm install"
    startCommand: "npm start"
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: "production"
      - key: MONGO_URI
        sync: false
 